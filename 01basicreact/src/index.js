import React from 'react';
import ReactDOM from 'react-dom/client';

import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
    <App />   // this gies make jsx which allow to make custom html in js

    // ane browser ma open karta view page source to package.json nu react-scripts ni maddadthi aa index.html ma add thai jaay che
)
